## 🚀 Usage

###  Provide a URL + its corresponding CSS file (recommended)

```bash
/extract-design-system *.md

Claude will:

Load the HTML structure from the URL (for component context)

Parse the provided CSS file (for complete style rules)

Merge both sources to extract a consistent design system (*.md file)

Put *.md into your Claude Design System, then to create new design project
